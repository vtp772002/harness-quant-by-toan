# References (llms.txt style) — Read When More Detail Is Needed

- pandas as-of join: use `merge_asof`; never use `shift(-N)` for future data.
- DuckDB point-in-time query: `WHERE ts <= $t ORDER BY ts`.
- Pydantic v2: parse at the boundary with `model_validate`.
